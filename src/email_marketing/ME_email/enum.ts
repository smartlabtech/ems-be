export enum EmailStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  DELETED = 'deleted',
}

export enum SortTypeEnum {
  ASCENDING = 'asc',
  DESCENDING = 'desc',
}

export enum EmailSource {
  IN = 'In',
  OUT = 'Out',
} 